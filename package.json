"scripts": {
  "start": "electron .",
  "build": "electron-builder"
}

function falar(texto) {
  const fala = new SpeechSynthesisUtterance(texto);

  fala.lang = "pt-BR"; // português
  fala.rate = 1; // velocidade
  fala.pitch = 1; // tom

  speechSynthesis.speak(fala);
}
